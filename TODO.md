# TODO
- pixi installable
- pyproject.toml or pixi.toml
- Better logo
- Config from tray icon
- Better tray UI

## Done
- Per-user config rather than per-user install (pure-push: the daemon starts
  config-less and follows the active seat user's config via kb-kill-push; the
  control socket is now mandatory, so "run daemon without socket" no longer applies)
