# TODO
- pixi installable
- pyproject.toml or pixi.toml
- run daemon without socket using command line arg
- Better logo
- Config from tray icon
- Better tray UI
- Per-user config rather than per-user install
